[alias]
    ack   = !git ls-files | ack --files-from=-
    au    = add --all
    bu    = !git rev-parse --abbrev-ref --symbolic-full-name @{u}
    cc    = clone
    ci    = commit
    cia   = commit --amend
    cm    = commit -m
    co    = checkout
    cp    = cherry-pick
    d     = diff
    dx    = diff --staged
    fe    = fetch --all
    hot   = "!f() { git log --all --find-copies --find-renames --name-only --format='format:' \"$@\" | grep -v '^$' | sort | uniq -c | sort -rn | less ;};f"
    ll    = log --pretty='%C(yellow)%h %C(cyan)%cd %Cblue%aN%C(auto)%d %Creset%s' --graph --date=relative --date-order
    lp    = log -p
    ls    = ls-files
    p     = pull
    pm    = pull origin master
    pu    = push
    pufo  = push --force
    pure  = pull --rebase
    rc    = rebase --continue
    re    = reset
    sc    = clone --depth=1
    st    = status -s -b
    count = shortlog -sn
