super+c
    icesh -f center

alt + Return
    urxvt

super+j
     chrome
super+p
     pass type
super+d
     icesh desktop

super+slash
           icesh -f windowlist
super+c
           icesh -f center
alt+slash
           rofi -show combi

super+b
          icesh -f if -P _NET_FRAME_EXTENTS=0 then bordered else borderless

super+Up
         icesh -f maximize

super+Down
	        icesh -f restore
super+End
	         icesh -f sizeto 49% 49% bottom left
super+Next
	        icesh -f sizeto 49% 49% bottom right
super+Begin
	       icesh -f sizeto 49% 49% center
super+Home
	        icesh -f sizeto 49% 49% top left
super+Prior
	       icesh -f sizeto 49% 49% top right

